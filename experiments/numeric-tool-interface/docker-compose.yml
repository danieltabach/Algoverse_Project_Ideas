services:
  experiment:
    build: .
    working_dir: /app
    environment:
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"

